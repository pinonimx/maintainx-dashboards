# .env.example — Copy this to .env and fill in your values.
# The .env file is gitignored and should NEVER be committed.

# MaintainX REST API token
# Get yours from: MaintainX → Settings → Integrations → REST API
MAINTAINX_API_KEY=your-api-token-here
